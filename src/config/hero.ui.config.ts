import type { HeroUIProviderProps } from '@heroui/react';

export const heroUIConfig: HeroUIProviderProps = {
  theme: 'light',
  defaultTheme: 'system',
  themes: ['light', 'dark', 'system'],
  skipFramerMotionAnimations: false,
};